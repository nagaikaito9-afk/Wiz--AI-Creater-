# AIゲーム作成 (Wiz AI Game Creator) 🧙‍♂️✨

AI賢者 **Wiz (ウィズ)** と共にWebゲームやPythonプログラムを対話形式で企画・制作・編集・リアルタイム実行できる次世代型ゲーム開発スタジオです。

---

## 🌟 主な特徴

### 1. 賢者AI「Wiz」との対話
- **名前の由来**: Wizard（賢者・魔法使い）。ゲーム開発、数学、プログラミング、演出の深い知識を持つ賢者。
- **親しみやすさ**: Google Geminiのように気さくでフレンドリーな話し相手。
- **2つのモード**:
  - 💬 **通常会話モード**: 雑談やゲームのアイデア出し、ルールの企画相談に最適。
  - ⚡ **プログラム作成モード**: WizがAntigravityのようにコードを生成し、右側のコードタブやファイルツリーに自律反映。

### 2. ファイル・フォルダ添付 (最大5個)
- ドラッグ＆ドロップまたはファイル選択から一度に最大5個までのファイル・フォルダを添付可能。
- ソースコードや画像アセットの内容をAIが自動解析して開発に活用。

### 3. 収納可能な右側コードタブ (Web IDE)
- ファイルツリー（新規ファイル作成、フォルダ作成、リネーム、削除）。
- リアルタイムシンタックスハイライト（HTML, CSS, JavaScript, Python, C++）。
- 行番号表示、タブ切り替え、Ctrl+Sによる保存。
- ワンクリックまたはショートカットキー `Ctrl+B` で瞬時に折りたたみ（収納）/ 展開が可能。

### 4. プログラム実行機能 (Runner)
- ヘッダーの **「実行」** ボタン、またはショートカット `Ctrl+Enter` で即座にプレビュー起動。
- デフォルトは `index.html`（HTML/CSS/JS）。相対パスのCSS/JS参照を自動バンドルして安全にiframe sandboxで動作。
- `.py` (Python) ファイルはブラウザ内WebAssembly (Pyodide) を用いてリアルタイムにコンソール実行。
- チャット内から「このプログラムを実行して！」「○○フォルダの中のindex.htmlを実行して！」と指示するだけでも即実行。

### 5. インテリジェント画面キャプチャ＆操作
- **「○○フォルダ内のindex.htmlを実行して『スタート』ボタンを押したら何が出てくるか画像を送って」** といった詳細リクエストにも対応！
- 指定要素をクリック・操作した後の状態を自動レンダリングし、スクリーンショット画像（PNG）をチャット内に投稿・解説してくれます。

---

## 🚀 使い方

1. `index.html` をブラウザ（Chrome, Edge等）で開きます。
2. ヘッダー上部で **「通常会話モード」** または **「プログラム作成モード」** を選択します。
3. チャット欄に作りたいゲームのイメージや相談を入力します。
   - 例: *「ブロック崩しゲームに必殺技ボタンを追加して！」*
   - 例: *「このプログラムを実行して！」*
   - 例: *「index.htmlを実行して「スタート」ボタンを押した結果を画像で送って」*
4. 右上の **「実行」** ボタンでいつでもゲームをプレイできます。
5. 作成したゲームは **「ZIP保存」** ボタンで丸ごとダウンロード可能です。

---

## ⌨️ ショートカットキー

- `Ctrl + B`: 右側コードタブの収納 / 展開
- `Ctrl + Enter`: プログラムの即時実行
- `Ctrl + S`: アクティブファイルの上書き保存
- `Enter`: チャットメッセージ送信 (Shift+Enterで改行)
- `Esc`: 実行プレビューモーダルを閉じる

---

## 📁 プロジェクト構成

```
ai-game-creator/
├── index.html              # メインアプリケーションUI
├── css/
│   ├── main.css            # サイバーミスティック・ダークテーマ / ガラスモーフィズム
│   └── editor.css          # コードエディタ・ファイルツリー・プレビュー画面スタイル
├── js/
│   ├── app.js              # メインコントローラー（UI・添付ファイル・レイアウト）
│   ├── wiz.js              # Wizペルソナプロンプト & Gemini APIクライアント
│   ├── fileSystem.js       # 仮想ファイルシステム (VFS) & バンドラー & ZIP保存
│   ├── editor.js           # コードエディタ・ファイルツリー・シンタックスハイライト
│   ├── runner.js           # 実行エンジン (HTML sandbox & Pyodide Python & キャプチャ)
│   └── agentActions.js     # Wizのアクション自動実行エンジン
├── cpp/
│   └── game_logic.cpp      # C++ゲームロジック・物理演算コア（ルール準拠）
└── README.md               # プロジェクト概要ドキュメント
```
